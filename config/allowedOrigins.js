//this file is used to define the allowed origins for the CORS policy
//you can add your frontend url here as orgin to allow access to the server
//by passing data to the server from the frontend as tokens or any other data

const allowedOrigins = [
    'http://localhost:3000',
]

module.exports = allowedOrigins;